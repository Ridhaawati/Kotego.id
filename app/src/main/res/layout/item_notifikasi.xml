<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/img_notif"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:scaleType="centerCrop"
                android:src="@drawable/placeholder_food" />

            <TableLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp">

                <TableRow android:layout_marginBottom="2dp">
                    <TextView android:text="Nama Menu " android:textSize="12sp" />
                    <TextView android:id="@+id/tv_nama_notif" android:text=": Ayam Chili padi" android:textStyle="bold" android:textSize="12sp" />
                </TableRow>

                <TableRow android:layout_marginBottom="2dp">
                    <TextView android:text="Harga " android:textSize="12sp" />
                    <TextView android:id="@+id/tv_harga_notif" android:text=": 25.000" android:textSize="12sp" />
                </TableRow>

                <TableRow android:layout_marginBottom="2dp">
                    <TextView android:text="Kategori " android:textSize="12sp" />
                    <TextView android:id="@+id/tv_kategori_notif" android:text=": Makanan" android:textSize="12sp" />
                </TableRow>

                <TableRow>
                    <TextView android:text="Status " android:textSize="12sp" />
                    <TextView android:id="@+id/tv_status_notif" android:text=": On" android:textSize="12sp" />
                </TableRow>
            </TableLayout>
        </LinearLayout>

        <Button
            android:id="@+id/btn_lihat_detail"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginTop="10dp"
            android:backgroundTint="#8D7B6D"
            android:text="Lihat Detail Pesanan"
            android:textAllCaps="false"
            android:textSize="12sp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>